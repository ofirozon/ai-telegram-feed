name: AI News -> Telegram

on:
  schedule:
    # 3 runs per day at 08:00 / 13:00 / 19:00 Israel time (IDT, summer).
    # GitHub Actions cron is UTC only; Israel = UTC+3 in summer, UTC+2 in winter,
    # so in winter the posts will appear one hour earlier (07:00 / 12:00 / 18:00).
    # Note: GitHub Actions schedules can be delayed by 5-15 minutes during peak load.
    - cron: "0 5,10,16 * * *"
  workflow_dispatch:       # also runnable manually from the Actions tab

permissions:
  contents: write          # needed so the job can commit seen.json back

concurrency:
  group: publish
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install deps
        run: pip install -r requirements.txt

      - name: Run publisher
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID:   ${{ secrets.TELEGRAM_CHAT_ID }}
          GEMINI_API_KEY:     ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL:       "gemini-2.5-flash-lite"
          MAX_ITEMS_PER_RUN:  "1"
          MAX_AGE_HOURS:      "48"
        run: python main.py

      - name: Commit updated seen.json
        run: |
          if [ -n "$(git status --porcelain seen.json)" ]; then
            git config user.name  "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add seen.json
            git commit -m "chore: update seen state"
            # Retry up to 3 times in case someone else pushed during this run
            for i in 1 2 3; do
              git pull --rebase origin main && git push && break
              echo "push failed, retrying ($i/3)"
              sleep 3
            done
          fi
